[project]
name = "rekha-shopper"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "langgraph>=0.2,<1",
  "httpx>=0.27",
]

[tool.uv]
package = false

[project.scripts]
shopper = "src.runner:main"
